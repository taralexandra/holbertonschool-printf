.TH _PRINTF 3 "July 2024" "1.0" "_printf man page"
.SH NAME
_printf \- a custom printf function for formatted output
.SH SYNOPSIS
.B #include "main.h"
.sp
.B int _printf(const char *format, ...);
.SH DESCRIPTION
.B _printf
produces output according to a format as described below.
.PP
The
.B format
argument is a character string composed of zero or more directives.
Each directive is introduced by the '%' character and is replaced by
a corresponding argument after the format string.
.PP
The function handles the following conversion specifiers:
.TP
.B %c
Prints a single character.
.TP
.B %s
Prints a string of characters.
.TP
.B %d
Prints a decimal (base 10) number.
.TP
.B %i
Prints an integer in base 10.
.TP
.B %%
Prints a literal '%' character.
.SH RETURN VALUE
.B _printf
returns the number of characters printed (excluding the null byte used to end output to strings).
.SH EXAMPLES
.PP
Here is an example of how to use
.B _printf:
.sp
.nf
    #include "main.h"
/**
  * main - tests.
  *
  * Return: 0
  */

    int main(void) {
        _printf("Character: %c\n", 'H');
        _printf("String: %s\n", "Hello, world!");
        _printf("Decimal: %d\n", 123);
        _printf("Integer: %i\n", 456);
        _printf("Percentage: %%\n");
        return 0;
    }
.fi
.SH SEE ALSO
.BR printf (3),
.BR putchar (3)
.SH AUTHOR
Tara QUARTERO <9531@holbertonstudents.com>
